_id: _design/zenwiki
views: 
  recently_touched_pages:
    map: |
      function(doc) {
        if (doc.type == 'page' && doc.updated_at) {
          var key = [doc.updated_at, doc._id];
          emit(key, null);
        }
      }
